The simulation can be run from com.kmichaelfox.qlearning.QLearning.

The program can be executed with an optional command line argument that specifies the number of grids to output. If no argument is provided, the program defaults to 40 randomly generated grids.